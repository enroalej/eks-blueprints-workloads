apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: jaeger-operator
  namespace: jaeger
  finalizers:
  - resources-finalizer.argocd.argoproj.io
spec:
  project: team-plugins
  source:
    repoURL: {{ .Values.repoUrl }}
    path: addons/jaeger-operator
    targetRevision: main
  destination:
    server: {{ .Values.destinationServer | default "https://kubernetes.default.svc" }}
    namespace: jaeger
  syncPolicy:
    automated:
      prune: true
    syncOptions:
    - CreateNamespace=true
    retry:
      limit: 1
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 1m